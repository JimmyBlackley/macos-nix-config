#!/bin/bash
# GPU usage - simplified for Apple Silicon (no sudo required)
# Note: Detailed GPU metrics require powermetrics with sudo
# This shows a placeholder or can be removed if not needed

sketchybar --set $NAME label="GPU"
